#include <iostream>
//9. Afficher les nombres de 1 à 10 avec une boucle `for`.  
using namespace std;

int main() {
    //for (condition de départ; jusqu'a ? ; itération)
    for (int i=0;i<11;i++) {
        cout << i << endl;
    }
}